in order to run the follwing container run these commands:
```bash
git clone https://github.com/OfirKedem/docker_conda_jupyter_ex.git
cd docker_conda_jupyter_ex
docker-compose up
```
Press the link in the log in order to enter jupyter.
